import 'package:flutter/material.dart';

class WebScreenSize extends StatelessWidget {
  const WebScreenSize({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Webview'),
      ),
    );
  }
}
